services:
  postgres:
    image: postgres:16
    container_name: teamme-pg
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: teamme
    ports:
      - "5432:5432"
